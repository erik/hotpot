TODO

More web endpoints
  - Vector tiles
  - Activity as GeoJSON
  - UI for filter + gradient creation

Customization
  - Vary gradient stops based on zoom level / max number of points in tile

Misc
  - Strip noisy points from tracks (e.g. inside buildings, etc.)
  - Export rendered tiles as PMTiles / MBTiles
